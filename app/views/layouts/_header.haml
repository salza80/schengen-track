%nav.navbar.navbar-inverse.navbar-fixed-top
  .container
    .row
      .col-md-12
      .navbar-header
        %button.navbar-toggle.collapsed{"aria-controls" => "navbar", "aria-expanded" => "false", "data-target" => "#navbar", "data-toggle" => "collapse", :type => "button"}
          %span.sr-only Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
        %a.navbar-brand{:href => "#"} 
          = image_tag("logo.svg", alt: "Schengen Visa Calculator", class: "pull-left")
        %span.navbar-text.site-title
          Schengen Visa Calculator
        - if current_person
          %p.navbar-text
            = current_person.full_name + " - " + current_person.nationality.name
            = link_to '(change)', my_details_path
      #navbar.collapse.navbar-collapse.pull-right
        %ul.nav.nav-pills
          / %li
          /   = link_to 'Travel Record', visits_path, class: 'navbar-link'
          %li.dropdown{:role => "presentation"}
            %a.dropdown-toggle{"aria-expanded" => "false", "data-toggle" => "dropdown", :href => "#", :role => "button"}
              Settings
              %span.caret
            %ul.dropdown-menu{:role => "menu"}
              %li
                = link_to 'My Details', my_details_path
              - if user_signed_in?
                %li
                  = link_to 'Account', edit_user_registration_path
          - if user_signed_in?
            %li
              = link_to 'Log out', destroy_user_session_path,  method: :delete,  class: 'navbar-link'
          - else 
            %li
              = link_to 'Login', new_user_session_path, class: 'navbar-link log-in-link'
    .row
      .col-md-12
        %ul.nav.nav-tabs.site-nav
          %li{:role => "presentation"}
            %a.navbar-link{href: visits_path}
              %span.glyphicon.glyphicon-plane{":aria-hidden" => "true"}
              Calculator
          %li{:role => "presentation"}
            %a.navbar-link{href: visits_path}
              %span.glyphicon.glyphicon-info-sign{":aria-hidden" => "true"}
              About


       
