name: PR Tests

on:
  pull_request:
    branches:
      - staging
    
jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run Tests
        uses: ./.github/actions/tests