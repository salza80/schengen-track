:plain
  // Load the form into the modal
  $('#visitModal .modal-body').html('#{j render("form")}');
  $('#visitModal .modal-title').text('#{t("visits.edit_title")}');
  $('#visitModal').modal('show');
  
  // Note: Cascade dropdowns are initialized by the script in _form.html.haml
